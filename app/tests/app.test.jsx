var expect = require('expect');


//describe improves the visual representation of the test data
describe('App', () => {
    it('should properly run tests', ()=> {
        expect(1).toBe(1);
    });
});